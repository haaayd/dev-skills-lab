<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Express Skills</title>
</head>
<body>
  <h1>You got em' Skills at <%= time %></h1>
  <ul>
    <% skills.forEach(skill => { %>
      <li>
        <form 
        action="/skills/<%= skill._id %>?_method=DELETE"
        class="delete-form"
        method="POST"
        >
        <button type="submit">X</button>
        </form>
        <a href="/skills/<%= skill._id %>"><%= skill.skill %></a> 
        -
        <%= skill.enjoyable ? "true" : "false"  %> 
      </li>
    <% }) %> 
  </ul>
  <a href="/skills/new">Add a Skill</a>
</body>
</html>